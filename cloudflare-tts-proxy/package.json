{
  "name": "german-tts-proxy",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that proxies Azure/Google TTS so the web app never ships the keys.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
